Update the CIA Platform landing page with the following improvements:
1. Update the Campaign Planner card description and tags:
Current description says "AI-powered content effectiveness assessment. Compare your content against database benchmarks." This is accurate but undersells it. Update to: "Evaluate your content against top performers. Get a data-backed verdict: deploy, refresh, or replace — with channel and timing recommendations."
Current tags include "Budget" — remove this since budget is optional and not a core feature. Replace the tags with: "AI Agent", "Compare", "Score", "Trends", "PDF Export"
Also remove "Templates" tag — we are not creating new content from templates.
2. Add an "Upload Content" quick action:
Add a fourth card or a prominent button/banner between the cards and the "How to use" section:

Label: "Upload & Evaluate Content"
Description: "Drop in a new content asset to compare it against your library and get an instant performance prediction."
This should link directly to the Campaign Planner intake form with the "Upload a new content asset" option pre-selected.
This is the most common action users will take — make it highly visible. It can be a full-width banner below the three cards instead of a fourth card if that looks cleaner.

3. Add a quick stats bar:
Between the greeting and the three cards, add a horizontal stats bar showing key numbers from the data at a glance. This makes the page feel alive and data-driven instead of static. Show 4-5 metrics:

Total content assets in library (e.g., "1,247 Assets")
Total leads tracked (e.g., "48.2K Leads")
Average lead-to-SQO conversion (e.g., "4.3% Avg Conversion")
Campaign plans created (e.g., "12 Plans")
Content freshness (e.g., "67% Fresh | 22% Aging | 11% Stale")

These numbers should pull from the actual dataset via the Data Agent. Keep the design minimal — small text, no heavy visuals, just clean numbers in a row.
4. Update the Content Library card:
Current description is fine but add a mention of the upload capability: "Browse all content assets by funnel stage, search by content ID, upload new assets, and preview URLs."
Update tags to include "Upload": "TOFU", "MOFU", "BOFU", "Search", "Upload"
5. Add recent activity or quick access section:
Below the three cards (and above "How to use"), add a "Recent Activity" section showing the last 3-5 actions:

"Campaign Plan: Sage Intacct — US NFP — TOFU PDF" — 2 hours ago — [Open]
"Content Uploaded: Fund Accounting Guide v2.pdf" — Yesterday — [View]
"Performance Report: Q1 MOFU Email Analysis" — 3 days ago — [Open]

This helps returning users pick up where they left off without navigating into each section.
6. Personalize the greeting:
If you have the user's name available, show "Good afternoon, [Name]" instead of just "Good afternoon." This is a small touch that makes the platform feel personal.
7. Update "How to use" to be more actionable:
Instead of a generic "How to use" button, consider changing it to a guided flow:

"Getting Started" — 3-step guide:

Step 1: "Upload your content or browse the library"
Step 2: "Start a campaign plan — the AI evaluates your content against historical data and current trends"
Step 3: "Download your Sage-branded campaign plan PDF"


Show this as a collapsible section or a small modal, not a full page redirect.

8. Minor visual tweaks:

The card borders are currently a subtle green outline. Consider making the border slightly brighter on hover to give better interactive feedback.
The "Explore →" links at the bottom of each card could be more prominent — use Brilliant Green (#00D657) text on hover.
Consider adding a subtle animated gradient in the background (similar to the Sage brand template's green glow effect) to make the page feel more premium. Keep it very subtle — just a slow-moving green-to-black gradient in the top or bottom area.